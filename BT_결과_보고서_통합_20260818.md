# BT(Behavior Tree) 결과 보고서 (통합본)

**작성**: 2026-08-18 — `md/BT_fallback_track_20260817.md` + `md/종합_판단_20260817.md`의 BT 섹션을 통합. 재검증 없이 두 문서의 기존 실측치를 그대로 인용함.

---

## 1. 배경

v35(RL 전체 재학습)의 전투 단계 결과가 나오기까지 시간이 오래 걸려서, "RL 결과가 안 좋을 경우의 제출용 대안"으로 BT를 병행 개발함.

- 소스: `C:\Users\kevin\AIP\AIP_DCS\`(BehaviorTree.CPP v3 기반, 대회측 배포 기본 템플릿)
- 작업 시작 시점: `Rule_team01.xml`의 근접/중거리/원거리 3개 우선순위 티어가 전부 `Task_Empty`(placeholder)로 비어있어 **실제 교전 로직이 처음부터 하나도 없는 상태**였음

### 규정 확인
`2026_AI_Pilot_Top_Gun_Challenge_규정집_v1.4.pdf` 부록 A FAQ Q4: "강화학습, 모방학습, 지도학습, **행동 트리**, 휴리스틱 룰 기반, 하이브리드 등 어떤 방법론이든 자유롭게 사용 가능" → **BT 제출은 규정상 100% 허용됨.**

---

## 2. 작업 내역 (시간순)

### 2-1. 빌드 인프라 수정
- `AIP_DCS.vcxproj`의 Release\|x64 PropertySheets import 경로 오류(`..\..\` → `..\`) 수정
- 존재하지 않는 `.lib`를 찾던 무관한 `LDFP_Model.props` import 제거
- 툴셋 미스매치(v143 지정, 로컬엔 v145만 있음) → `/p:PlatformToolset=v145`로 오버라이드 빌드

### 2-2. 신규 교전 로직 구현
- `Task_WEZAttack`(≤914m, 근접 조준), `Task_MidRangeEngage`(≤6000m, 6시 방향 추적), `Task_LongRangeApproach`(>6000m, 원거리 접근) 3개 C++ Task 노드 신규 작성
- `Rule_team01.xml`의 `Task_Empty` 자리를 교체해서 실제 교전 트리 완성

### 2-3. 반복 튜닝 (8+ 사이클)
| 순서 | 문제 | 조치 |
|---|---|---|
| 1 | 저고도 자멸 | `EmergencyPullUp` 회피각 확대(atan 37°→75°), 트리거 고도 450→650m |
| 2 | WEZ 안에서도 조준각 안 좁혀짐(26°) | 순수 추적 → LOS 변화율 기반 예측(PN) 조준으로 교체 |
| 3 | WEZ 체류시간 0.6%(스쳐 지나가기만 함) | 중거리에서 상대 6시 방향(stern conversion) 조준으로 교체 |
| 4 | **초반 1회 교차 후 200~300초 내내 재교전 실패** | 원거리 리드타임이 40~50초까지 나오는 버그 발견·수정(최대 3초로 캡) — 가장 중요한 튜닝 버그 |
| 5 | 그래도 재교전까지 시간 오래 걸림 | 원거리 접근에 "리드턴" 편향 추가(효과 불명확) |

### 2-4. ⭐ 핵심 발견: 좌표 변환 버그 (가장 결정적인 수정)
- `LibMain.cpp`의 `ChangeData()`가 **위경도(도 단위)를 실제 미터 좌표로 변환하지 않고 그대로 사용**(변환 함수 `LLAtoCartesian()`은 있었으나 호출 누락)
- BT 전체(신규 노드 + 기존 SelectTarget/CheckSight 등)가 "실제로는 수천 m 떨어진 두 기체를 고도차만큼(수십 m)만 떨어진 걸로 착각"하며 작동
- **수정 후 처음으로 실제 데미지 발생 확인**: 47.5초 만에 양쪽 동시 격추(규정상 무승부 처리 대상)

### 2-5. 방어 로직 신설
- `Task_Evade`(급선회+거리 벌기) + `DECO_ThreatCheck`(상대가 나를 조준 중인지 판단) 신규 작성
- 위협 판정 거리 3000m → 1100m로 축소(너무 일찍 발동해 교전 자체를 회피해버리는 문제 수정)

### 2-6. 좌표 버그 수정 후 재튜닝
- `Task_WEZAttack`의 리드 시간을 `Los_Degree`(현재 조준 오차)에 비례한 적응형으로 조정 → 재교전 시 명중률 개선(첫 패스만 명중 → 여러 패스에서 명중)

---

## 3. 실측 결과

### 3-1. 1차 검증 — 6개 시나리오 (교전각 0°/45°/90° × 시드 2개, 200초)
| 시나리오 | 결과 |
|---|---|
| 0°, seed1 | **승(HP 우위, 0.997 vs 0.961)** |
| 0°, seed2 | 승(상대 자멸) |
| 45°, seed1 | 승(상대 자멸) |
| 45°, seed2 | 승(상대 자멸) |
| 90°, seed1 | **패(우리 쪽 자멸)** |
| 90°, seed2 | 승(상대 자멸) |

→ 6개 중 5개 무패지만, 승리의 대부분(4/5)이 진짜 격추가 아니라 **상대 자멸**에 의한 것. 진짜 실력승은 1건뿐.

### 3-2. 2차 검증 — 60개 시나리오 배치 (교전각 6종 × 시드 10개, `bt_batch_verify.py`, 대회 규정 기준 판정)
| 버전 | 승 | 패 | 무 |
|---|---:|---:|---:|
| 고도 유지 보정 없음(기준) | **50.0%** | **43.3%** | 6.7% |
| 고도 보정 과함(3000m/계수0.5) | 15.0% | 10.0% | **75.0%**(무교전 폭증) |
| 고도 보정 완화(1200m/계수0.15) | 검증 미완료(부분 표본 18/60, 기준선과 거의 동일해 보이나 결론 못 냄) |

→ 고도 보정을 강하게 넣으면 자멸은 줄지만 교전 자체가 사라지고, 약하게 넣으면 효과가 불확실한 딜레마 상태. **현재 가장 신뢰할 수 있는 수치는 "고도 보정 없음" 버전의 승 50.0% / 패 43.3% / 무 6.7%.**

### 3-3. RL(v35) 트랙과의 비교 (2026-08-17 12:35 기준)
| | RL(v35, stage15/16) | BT |
|---|---|---|
| WEZ 진입율 | 100% | 자멸 제외 시 대부분 교전 성립 |
| 승/패/무 | 승 0 / 패 0 / 무 41 / 자멸 19 (stage16, 60ep) | 승 50.0% / 패 43.3% / 무 6.7% (60ep) |
| 자멸률 | 31.7% (19/60) | 43.3% |
| 검증 성숙도 | 학습 진행형, 표본 적음 | 표본 적당(60×2), 최신 고도보정 수정은 미검증 |

**공통 병목**: 두 트랙 다 "자멸(고도 추락)"이 핵심 문제로 수렴. RL은 31.7%, BT는 43.3%.

---

## 4. 최종 산출물

- 빌드 산출물: `C:\Users\kevin\AIP\bin\Release.x64\AIP_DCS.dll` → 배포용 사본 `DogFightEnv\Release\AIP_DCS_team01.dll`
- 룰: `DogFightEnv\Release\Rule_team01.xml`
- 제출 설정: `student/my_submission.py`의 `BT_DLL`/`BT_RULE_XML` 반영 완료 (`MODE`는 `"hybrid"` 유지, 최종 전환 여부는 RL 결과 확정 후 결정 필요)

### 수정된 파일 목록
- `AIP_DCS/AIP_DCS.vcxproj`, `.vcxproj.filters`
- `AIP_DCS/LibMain.cpp` (좌표 변환 버그 수정)
- `AIP_DCS/BehaviorTree/CPPBehaviorTree.cpp` (신규 노드 등록)
- `AIP_DCS/BehaviorTree/BT_Content/Task/Task_WEZAttack.*`, `Task_MidRangeEngage.*`, `Task_LongRangeApproach.*`, `Task_Evade.*` (신규)
- `AIP_DCS/BehaviorTree/BT_Content/Task/Task_EmergencyPullUp.cpp` (회피각 조정)
- `AIP_DCS/BehaviorTree/BT_Content/Decorator/DECO_ThreatCheck.*` (신규)
- `AIP_DCS/BehaviorTree/BT_Content/Task/TaskNodes.h`, `Decorator/DecoratorNodes.h` (헤더 등록)
- `DogFightEnv/Release/Rule_team01.xml` (교전 트리)
- `DogFightEnv/Release/run_local_dogfight.py` (시나리오 변형 CLI 옵션 추가, 승인 받고 수정)
- `DogFightEnv/Release/student/my_submission.py` (제출 설정)

---

## 5. 알려진 한계 (제출 전 반드시 인지해야 할 것)

1. **자멸 리스크가 핵심 병목** — 60개 배치 기준 43.3%. 90°/seed1 케이스 로그 분석 결과 단순 추락이 아니라 Roll이 -138°→165°→-174°로 요동치는 자세 이탈(스핀에 가까움). `EmergencyPullUp` 회피 벡터를 더 수직 위주로 바꿔봤으나 효과 없음 — 반복된 급기동으로 에너지(속도) 소진 → 실속에 가까운 상태로 추정. 자세(조준점)만 바꿔서 해결되는 문제가 아니라 속도/받음각 고려 로직 필요 → **미해결**.
2. **고도 보정 튜닝 딜레마 미해결** — 강하게 넣으면 무교전 폭증(75%), 완화 버전은 검증 중단(부분 표본만 존재).
3. **1차 검증(6개 시나리오)의 승리 대부분이 상대 자멸** — 진짜 실력승 검증 부족(1/6).
4. **자기 자신과의 미러전 검증이 대부분** — 실제 대회 상대(다른 전략)와는 미검증.
5. **실제 제출 경로(언리얼 서버 UDP) 미검증** — 로컬 JSBSim 하네스로만 테스트, 로컬에 언리얼 서버 없음.
6. **정밀 조준(≤2°) 안정적 성공률**도 아직 초기 수준.

---

## 6. 다음 단계 제안

1. RL(v35) stage 17~20 결과가 나오면 BT와 비교해서 최종 제출 방식(RL/BT/하이브리드) 결정
2. BT로 갈 경우: 자멸 리스크(스핀 회복 로직) 먼저 해결 → 고도 보정 재검증(1200m/0.15) 마무리 → 표본 확대로 통계적 신뢰도 확보
3. 가능하면 실제 언리얼 서버 경로로 최소 1회 검증
4. 마감(2026-08-23) 기준 늦어도 8/20~21경 RL/BT 중 하나로 확정하고 제출 준비 착수

---

*본 문서는 `md/BT_fallback_track_20260817.md`와 `md/종합_판단_20260817.md`의 실측 내용을 통합 정리한 것이며, 통합 과정에서 별도 재검증은 수행하지 않음.*
